#include<stdio.h>
void main()
{
    int a;
    char b;
    float c;
    double d;

    printf("\n Size of int data type is => %d", sizeof(a));
    printf("\n Size of char data type is => %d", sizeof(b));
    printf("\n Size of float data type is => %d", sizeof(c));
    printf("\n Size of double data type is => %d", sizeof(d));
}

